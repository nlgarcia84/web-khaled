import blockContent from './blockContent'
import campaign from './campaign'
import category from './category'
import post from './post'

export const schemaTypes = [campaign, category, post, blockContent]
